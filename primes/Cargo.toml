[package]
name = "primes"
version = "0.1.0"
edition = "2024"

[dependencies]
fastrand = "2.4.1"
rand = { version = "0.10.1", features = ["sys_rng"] }
rand_chacha = "0.10.0"
uint = "0.10.0"

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "rand256p"
harness = false
